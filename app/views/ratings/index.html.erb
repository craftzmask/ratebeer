<h2>List of ratings</h2>

<ul>
  <% @ratings.each do |rating| %>
    <li>
      <%= render rating %>
      <%= button_to "delete", rating, method: :delete, form: { data: { "turbo-confirm": "Delete #{rating}?" } } %>
    </li>
  <% end %>
</ul>

<%= link_to "New Rate", new_rating_path %>